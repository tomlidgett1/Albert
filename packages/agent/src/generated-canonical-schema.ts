// Generated by `npm run registry:write` from registry 1.8.0. Do not hand-edit.

/** The canonical model as the agent sees it before writing SQL. */
export const CANONICAL_SCHEMA_DOC = "mart.commerce_sales_event — grain key id, time business_date\n  measures: signed_quantity, gross_amount, discount_amount, sale_amount_inc_tax, signed_net_amount_inc_tax, signed_net_amount_ex_tax, signed_total_cost, refund_amount_inc_tax\n  attributes: id, event_kind, sale_order_id, location_id, register_id, channel_id, product_variant_id, product_category_id, customer_account_id, worker_id, business_date, voided, internal_transaction, currency, order_status, cost_observed\n  joins: core.location ON location.id = location_id (location: name); core.register ON register.id = register_id (register: name); core.channel ON channel.id = channel_id (channel: name); core.product_category ON product_category.id = product_category_id (product.department: name); core.product_category ON product_category.id = product_category_id (product.category: name); core.product_variant ON product_variant.id = product_variant_id (product.variant: name); core.customer_account ON customer_account.id = customer_account_id (customer: display_name); core.worker ON worker.id = worker_id (worker: display_name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.customer_order_activity — grain key id, time business_date/last_order_at\n  measures: net_amount_ex_tax\n  attributes: id, customer_account_id, location_id, channel_id, business_date, last_order_at, customer_order_number, period_order_count\n  joins: core.location ON location.id = location_id (location: name); core.channel ON channel.id = channel_id (channel: name); core.customer_account ON customer_account.id = customer_account_id (customer: display_name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.inventory_health_day — grain key id, time snapshot_date/business_date\n  measures: quantity_on_hand, stock_value, trailing_daily_units_sold, units_sold, opening_units, received_units, cost_of_goods_sold, stocktake_variance_units\n  attributes: id, snapshot_date, business_date, stock_location_id, product_variant_id, product_category_id, currency\n  point-in-time levels (never SUM across dates): quantity_on_hand, stock_value\n  joins: core.stock_location ON stock_location.id = stock_location_id (stock_location: name); core.product_category ON product_category.id = product_category_id (product.department: name); core.product_category ON product_category.id = product_category_id (product.category: name); core.product_variant ON product_variant.id = product_variant_id (product.variant: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.workforce_day_worker_location — grain key id, time business_date\n  measures: rostered_minutes, worked_minutes, overtime_minutes, labour_cost\n  attributes: id, business_date, location_id, worker_id, currency\n  joins: core.location ON location.id = location_id (location: name); core.worker ON worker.id = worker_id (worker: display_name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.finance_day_location — grain key id, time business_date\n  measures: accrued_revenue, cash_receipts, operating_expenses, cost_of_sales, other_net_expenses, gst_collected, gst_paid, receivables_outstanding, payables_outstanding\n  attributes: id, business_date, location_id, legal_entity_id, gl_account_id, tax_code_id, currency\n  point-in-time levels (never SUM across dates): receivables_outstanding, payables_outstanding\n  joins: core.location ON location.id = location_id (location: name); core.legal_entity ON legal_entity.id = legal_entity_id (legal_entity: name); core.gl_account ON gl_account.id = gl_account_id (gl_account: name); core.tax_code ON tax_code.id = tax_code_id (tax_code: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.workforce_sales_aligned — grain key id, time business_date\n  measures: labour_cost, worked_hours, net_sales_ex_gst, gross_margin\n  attributes: id, business_date, location_id, worker_id\n  joins: core.location ON location.id = location_id (location: name); core.worker ON worker.id = worker_id (worker: display_name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.merchandising_aligned — grain key id, time business_date\n  measures: net_sales_ex_gst, gross_margin, units_sold, stock_on_hand_units, stock_on_hand_value\n  attributes: id, business_date, product_category_id, product_variant_id\n  joins: core.product_category ON product_category.id = product_category_id (product.department: name); core.product_category ON product_category.id = product_category_id (product.category: name); core.product_variant ON product_variant.id = product_variant_id (product.variant: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.reconciliation_aligned — grain key id, time business_date\n  measures: pos_net_sales_ex_gst, ledger_accrued_revenue, net_sales_ex_gst, accrued_revenue\n  attributes: id, business_date, location_id\n  joins: core.location ON location.id = location_id (location: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.settlement_reconciliation_aligned — grain key id, time business_date/settlement_date\n  measures: pos_tender_amount, bank_settlement_amount, tender_amount, cash_receipts, settlement_variance, tender_count, bank_transaction_count, linked_tender_count\n  attributes: id, business_date, settlement_date, location_id, currency, evidence_status\n  joins: core.location ON location.id = location_id (location: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\ncore.commerce_payment — grain key id, time business_date\n  measures: amount\n  attributes: id, location_id, channel_id, business_date, status, currency\n  joins: core.location ON location.id = location_id (location: name); core.channel ON channel.id = channel_id (channel: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\ncore.finance_bank_transaction — grain key id, time business_date\n  measures: amount\n  attributes: id, location_id, legal_entity_id, business_date, status, currency\n  joins: core.location ON location.id = location_id (location: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.sales_day — grain key id, time business_date\n  measures: gross_takings_inc_gst, net_sales_ex_gst, gross_margin, units_sold, transactions, refund_amount_inc_tax, discount_amount\n  attributes: id, business_date\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\nmart.sales_day_location_category — grain key id, time business_date\n  measures: gross_takings_inc_gst, net_sales_ex_gst, gross_margin, units_sold, transactions, refund_amount_inc_tax, discount_amount\n  attributes: id, business_date, location_id, product_category_id\n  joins: core.location ON location.id = location_id (location: name); core.product_category ON product_category.id = product_category_id (product.department: name); core.product_category ON product_category.id = product_category_id (product.category: name)\n  never joined raw to another fact table: aggregate each side in its own subquery first, or use the aligned mart\n\ndimension tables — join on their `id` column; the exact label columns are:\n  core.channel: id, name\n  core.customer_account: id, display_name\n  core.gl_account: id, name\n  core.legal_entity: id, name\n  core.location: id, name\n  core.product_category: id, name\n  core.product_variant: id, name\n  core.register: id, name\n  core.stock_location: id, name\n  core.tax_code: id, name\n  core.worker: id, display_name";
